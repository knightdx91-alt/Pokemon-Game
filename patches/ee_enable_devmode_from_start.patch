diff --git a/src/start_menu.c b/src/start_menu.c
index 819c3e08..4e766612 100644
--- a/src/start_menu.c
+++ b/src/start_menu.c
@@ -59,6 +59,7 @@
 extern u8 RDB_StartMenuBetaOptionBootstrap[];
 extern u8 RyuDebugMenuBootstrap[];
 extern u8 RyuBetaMenuBootstrap[];
+extern u8 Ryu_enableDevMode[];
 
 extern const u8 gText_RyuVersion[];
 
@@ -1416,6 +1417,18 @@ static bool8 HandleStartMenuInput(void)
             ScriptContext1_SetupScript(RyuDebugMenuBootstrap);
             return TRUE;
         }
+        else if (JOY_HELD(L_BUTTON))
+        {
+            // Hold L + SELECT in the start menu to enable Dev Mode from the
+            // very start (reuses EE's own "Enable Dev Mode?" confirmation).
+            RemoveExtraStartMenuWindows();
+
+            HideStartMenu();
+            HideFieldMessageBox();
+            ScriptContext2_Enable();
+            ScriptContext1_SetupScript(Ryu_enableDevMode);
+            return TRUE;
+        }
         else
         {
             RemoveExtraStartMenuWindows();
